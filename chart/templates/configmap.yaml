apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "peithos.fullname" . }}-config
  labels:
    {{- include "peithos.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
